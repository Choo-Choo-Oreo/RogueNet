# Minions

Every creature JSON here is a minion. Ids are file names and must be unique across the whole
`entities.antagonist/` tree. A species can be grouped in a subfolder if it has variants. The
format, the tags and the full list are in [`../README.md`](../README.md).
