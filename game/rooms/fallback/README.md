# Fallback

Rooms used when a biome has no room of a role (entrance, boss, treasure, corridor, normal),
plus the default `defines.json` (room count, tag weights, music, a `monsters` table). It is
not a biome you pick and it is not a look; the generator backfills from here. The room file
format is in [`../README.md`](../README.md).
